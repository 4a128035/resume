# 🚀 王鐘賢 Ray 的個人履歷 - 更新與發佈標準作業程序 (SOP)

這份文件記錄了本履歷專案在本地更新、提交，並部署發佈至 GitHub Pages 的標準工作流程。

---

## 👥 團隊協作分工模式 (Workflow Mode)

本專案採用 **「AI 自動化打包建置」+「MIS 人工安全審核發佈」** 的高效 SecOps 模式：
* 🤖 **AI 助理責任**：負責修改 `index.html` 網頁與 `README.md` 文件、進行 RWD 與排版優化、**自動執行 `git add` 並撰寫規範的 `git commit`**。
* 👨‍💻 **您的責任 (最高權限)**：手動執行 **`git push`** 指令，負責安全驗證並將代碼正式發佈上線。

---

## 🛠️ 每一次更新履歷的標準步驟 (Step-by-Step)

### 📌 步驟一：交給 AI 修改與 Commit
當您需要修改履歷（例如：更新證照、專案或改錯字）時：
1. 直接在對話中告知我修改需求。
2. 我會修改好您的網頁，並在本地自動幫您執行 Git 提交：
   ```bash
   git add index.html README.md
   git commit -m "style/feat: <更新說明>"
   ```
3. 我會向您回報：`「我已經在本地 Commit 完成囉！您可以進行發佈了。」`

---

### 📌 步驟二：由您手動發佈上線 (Push)

當收到 AI 回報 Commit 完成後，請您開啟 Mac 的 **終端機 (Terminal)**，執行以下步驟：

#### 1. 移動到專案路徑
確認終端機是否在您的專案資料夾底下（若已經在裡面，可跳過此步）：
```bash
cd /Users/ray/Desktop/resume-project
```

#### 2. 執行推送指令
輸入以下 Git 推送指令並按下 `Enter`：
```bash
git push
```

#### 3. 身分安全驗證（如遇要求輸入）
如果終端機跳出提示，要求您輸入使用者名稱與密碼，請填入：
* **Username**: `4a128035`
* **Password (Token)**: 請貼上您之前產生的 `github_pat_...` 開頭的細粒度存取權杖 (Personal Access Token)。
  * 🔑 **資安與 MIS 提示**：貼上密碼時，終端機畫面**不會**顯示任何字元（這代表您的 Token 正在受到保護，不會洩漏在螢幕上），請在貼上後直接按 `Enter`。

---

### 📌 步驟三：自動化 CDN 建置與驗證 (Verify)

只要終端機跑完百分比並顯示成功，GitHub Pages 就會自動觸發背景建置流程（GitHub Actions）。

* **等待時間**：約 **15 ~ 30 秒**。
* **驗證網址**：在瀏覽器打開您的專屬官方履歷網址，即可看到最新的更新！
  👉 **[https://4a128035.github.io/resume/](https://4a128035.github.io/resume/)**

---

## 🛡️ 資安守則 (Security Rules)

1. **不可硬編碼憑證**：本專案為公開儲存庫，**絕對不能**在 `index.html` 或任何代碼中寫入您的真實密碼、伺服器私鑰或 API Keys。
2. **本地工具排除**：`input.css`、`.DS_Store` 與 Mac 專用的二進制執行檔已自動設為忽略，請勿手動將其 push 到 GitHub 上，以維持專案純淨度。
3. **金鑰妥善保存**：您的 `github_pat_...` Token 是您對 GitHub 的最高控制權限，請將其儲存在安全的密碼管理器中，絕不外流。
